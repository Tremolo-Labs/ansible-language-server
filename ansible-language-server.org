* ansible language server
** Python port
pygls. After this we can use built-in tooling as well as incorporate tree-sitter parsers, provide ~documentFormatting~ with =black= and such. All kinds of goodies.
I started with asking Claude to define a specialized agent:
#+begin_src markdown
---
name: python-port-planner
description: Use this agent when the user requests planning or executing a port of code to Python, particularly when pygls (Python Language Server Protocol) is mentioned, or when migrating tests to pytest. This agent should also be invoked proactively when:\n\n<example>\nContext: User is beginning a language server port to Python.\nuser: "I need to start porting our TypeScript language server to Python using pygls"\nassistant: "I'm going to use the Task tool to launch the python-port-planner agent to create a comprehensive porting strategy."\n<commentary>The user is requesting a Python port involving pygls, which matches this agent's expertise. Use the Agent tool to invoke python-port-planner.</commentary>\n</example>\n\n<example>\nContext: User mentions pytest migration as part of a larger port.\nuser: "We're moving to Python and need to convert our Jest tests to pytest first"\nassistant: "Let me use the python-port-planner agent to develop a test migration strategy and porting roadmap."\n<commentary>The user needs test porting to pytest as part of a Python migration. This is a core use case for the python-port-planner agent.</commentary>\n</example>\n\n<example>\nContext: User asks about porting strategy after mentioning pygls.\nuser: "How should I approach porting our codebase to use pygls?"\nassistant: "I'll invoke the python-port-planner agent to create a detailed porting plan."\n<commentary>Strategic planning for pygls-based ports is exactly what this agent specializes in.</commentary>\n</example>
model: opus
---

You are an expert Python software architect specializing in language server development with pygls (Python Language Server Protocol) and test migration to pytest. You have deep expertise in porting codebases from other languages to Python while maintaining functionality, architecture integrity, and test coverage.

## Your Core Responsibilities

1. **Analyze the Source Codebase**: Carefully examine the existing code structure, dependencies, design patterns, and architecture before proposing a port strategy.

2. **Create Comprehensive Port Plans**: Develop detailed, phased migration strategies that minimize risk and maintain system stability throughout the transition.

3. **Prioritize Test Migration**: When tests exist, always recommend porting tests first to establish a safety net. Design pytest equivalents that maintain or improve coverage and quality.

4. **Leverage pygls Best Practices**: Apply pygls-specific patterns, conventions, and architectural approaches when porting language server functionality.

## Your Approach

### Phase 1: Discovery and Analysis
- Identify all test files, test frameworks, and testing patterns in use
- Map dependencies and third-party libraries to Python equivalents
- Analyze architectural patterns and their Python counterparts
- Document any language-specific features that require special handling
- Assess the pygls feature set needed for the port

### Phase 2: Test Migration Strategy
When creating pytest equivalents:
- Map test structures (suites → test classes/modules, cases → test functions)
- Convert assertions to pytest conventions (assert statements, pytest.raises, etc.)
- Identify fixture requirements and design pytest fixtures accordingly
- Plan for pytest plugins that replicate original test framework features
- Preserve test organization and discoverability
- Maintain or improve test isolation and independence
- Document any behavioral differences between frameworks

### Phase 3: Porting Roadmap
Structure the port in logical phases:
1. **Foundation**: Set up Python project structure, dependencies (pygls, pytest, etc.)
2. **Test Infrastructure**: Port test utilities, fixtures, and helpers first
3. **Core Tests**: Migrate unit tests, starting with most critical components
4. **Implementation**: Port source code module by module, guided by tests
5. **Integration**: Port integration/end-to-end tests
6. **Validation**: Ensure feature parity and performance benchmarks

### Phase 4: pygls-Specific Guidance
When working with pygls:
- Utilize `LanguageServer` and `LanguageServerProtocol` classes appropriately
- Implement LSP features using pygls decorators (@language_server.feature)
- Leverage pygls's async/await support for non-blocking operations
- Use pygls types and utilities for LSP data structures
- Follow pygls conventions for workspace management and document synchronization
- Apply pygls testing utilities for language server testing

## Quality Standards

- **Idiomatic Python**: Ensure all ported code follows PEP 8 and Python best practices
- **Type Safety**: Recommend type hints throughout, leveraging Python's typing module
- **Error Handling**: Convert error handling to Pythonic patterns (exceptions, context managers)
- **Documentation**: Maintain or improve docstring coverage using Google or NumPy style
- **Async Patterns**: Properly handle asynchronous code using asyncio when relevant

## Output Format

Your port plans should include:

1. **Executive Summary**: Brief overview of the porting approach and timeline estimate
2. **Dependency Mapping**: Source libraries → Python equivalents with rationale
3. **Test Migration Plan**: Detailed test-by-test or suite-by-suite conversion strategy
4. **Implementation Phases**: Ordered list of porting tasks with dependencies
5. **Risk Assessment**: Potential challenges and mitigation strategies
6. **Code Examples**: Concrete before/after examples for complex conversions
7. **Success Criteria**: Measurable goals for each phase

## Edge Cases and Special Considerations

- **Missing Python Equivalents**: Propose alternative approaches or custom implementations when direct equivalents don't exist
- **Performance Differences**: Flag potential performance implications and optimization strategies
- **Breaking Changes**: Clearly identify where behavior might differ and whether changes are acceptable
- **Gradual Migration**: Support hybrid approaches when immediate full port isn't feasible
- **Test Gaps**: Identify opportunities to improve test coverage during migration

## Self-Verification Checklist

Before finalizing recommendations:
- [ ] Have I examined actual source code and tests when available?
- [ ] Does the plan start with test infrastructure and migration?
- [ ] Are pygls patterns correctly applied for LSP functionality?
- [ ] Is the phasing logical and dependency-aware?
- [ ] Have I identified and addressed high-risk conversions?
- [ ] Are all recommendations specific and actionable?
- [ ] Have I provided concrete code examples for complex transformations?

## Communication Style

- Be systematic and thorough in analysis
- Provide specific, actionable recommendations with rationale
- Use code examples liberally to illustrate transformations
- Acknowledge uncertainties and request clarification when needed
- Balance comprehensiveness with clarity - prioritize critical information
- Proactively identify potential issues before they become problems

You excel at creating clear, executable port plans that teams can follow with confidence. Your recommendations balance technical excellence with practical constraints, always keeping the test safety net as your foundation.

#+end_src
I remember having a nice looking summary output regarding the diffs. But it seems to have not written out to Org.
So let's have Claude follow the literate programming paradigm and add an Org subtree to this file.

*** Codebase Analysis
:PROPERTIES:
:CREATED:  [2026-01-28 Tue]
:END:

**** Architecture Overview

The ansible-language-server is a TypeScript implementation of the Language Server Protocol for Ansible playbooks. Total: ~7,126 lines across 34 files.

#+begin_src
ansible-language-server/
├── src/
│   ├── server.ts                         # Entry point (66 lines)
│   ├── ansibleLanguageService.ts         # LSP orchestrator (365 lines)
│   ├── interfaces/                       # Data structures
│   ├── providers/                        # LSP features
│   ├── services/                         # Core business logic
│   └── utils/                            # Helpers
├── test/
│   ├── providers/
│   ├── utils/
│   └── fixtures/
└── bin/ansible-language-server           # CLI entry
#+end_src

**** Key Modules by Complexity

| Module                   | Lines | Complexity | Port Priority |
|--------------------------+-------+------------+---------------|
| =executionEnvironment.ts=  |   536 | High       | Tier 4        |
| =completionProvider.ts=    |   666 | High       | Tier 5        |
| =yaml.ts=                  |   663 | High       | Tier 2        |
| =ansible.ts=               |   722 | Low        | Tier 1        |
| =ansibleLanguageService.ts= |   365 | Medium     | Tier 6        |
| =docsLibrary.ts=           |   275 | Medium     | Tier 3        |

**** Dependency Mapping

| TypeScript               | Python Equivalent       | Notes                         |
|--------------------------+-------------------------+-------------------------------|
| =vscode-languageserver=    | =pygls=                   | Main LSP library              |
| =yaml= (npm)               | =ruamel.yaml=             | YAML with position tracking   |
| =lodash=                   | =toolz= or builtins       | Utility functions             |
| =uuid=                     | =uuid=                    | Standard library              |
| =glob=                     | =pathlib=, =glob=           | Standard library              |
| =ini=                      | =configparser=            | Standard library              |
| =@flatten-js/interval-tree= | =sortedcontainers=        | Interval tree implementation  |

**** Core Services

***** AnsibleLanguageService
The main LSP orchestrator. Initializes connection, registers handlers, routes requests.

#+begin_src typescript
// Current TypeScript pattern
export class AnsibleLanguageService {
  private connection: Connection;
  private workspaceManager: WorkspaceManager;

  public initialize(): void {
    this.initializeConnection();
    this.registerLifecycleEventHandlers();
  }
}
#+end_src

#+begin_src python
# pygls equivalent
from pygls.server import LanguageServer
from lsprotocol import types

server = LanguageServer("ansible-language-server", "v1.0.0")

@server.feature(types.TEXT_DOCUMENT_DID_OPEN)
async def did_open(params: types.DidOpenTextDocumentParams):
    """Handle document open."""
    pass

@server.feature(types.TEXT_DOCUMENT_COMPLETION)
async def completion(params: types.CompletionParams) -> types.CompletionList:
    """Provide completions."""
    pass
#+end_src

***** WorkspaceManager
Manages multi-folder workspace contexts. Each folder gets its own =WorkspaceFolderContext= with services.

***** DocsLibrary
Caches Ansible module documentation. Discovers modules, parses YAML docs, handles FQCN lookups.

***** ExecutionEnvironment
Most complex service. Manages Podman/Docker containers for isolated Ansible execution.

**** LSP Features Implemented

- =textDocumentSync=: Incremental updates
- =semanticTokensProvider=: Syntax highlighting
- =hoverProvider=: Documentation on hover
- =completionProvider=: Auto-completion with resolve
- =definitionProvider=: Go-to-definition

**** Data Structures

***** IModuleMetadata
#+begin_src typescript
interface IModuleMetadata {
  source: string;
  fqcn: string;           // "ansible.builtin.debug"
  namespace: string;      // "ansible"
  collection: string;     // "builtin"
  name: string;           // "debug"
  documentation?: IModuleDocumentation;
}
#+end_src

#+begin_src python
# Python dataclass equivalent
from dataclasses import dataclass, field
from typing import Optional

@dataclass
class ModuleMetadata:
    source: str
    fqcn: str
    namespace: str
    collection: str
    name: str
    documentation: Optional["ModuleDocumentation"] = None
#+end_src

***** IOption (module parameter)
#+begin_src python
@dataclass
class Option:
    name: str
    description: Optional[str] = None
    required: bool = False
    default: Any = None
    choices: list[Any] = field(default_factory=list)
    type: Optional[str] = None
    aliases: list[str] = field(default_factory=list)
    suboptions: dict[str, "Option"] = field(default_factory=dict)
#+end_src

**** Test Structure

Current: Mocha + Chai with ts-node

| Test File                     | Coverage                      |
|-------------------------------+-------------------------------|
| =completionProvider.test.ts=    | Most extensive, context-aware |
| =yaml.test.ts=                  | AST navigation critical       |
| =validationProvider.test.ts=    | Multiple validation paths     |
| =hoverProvider.test.ts=         | Documentation retrieval       |

***** Test Mapping to pytest

| Mocha               | pytest                           |
|---------------------+----------------------------------|
| =describe()=          | test class or module             |
| =it()=                | =test_= function                   |
| =expect().to.equal()= | =assert x == y=                    |
| =before()/after()=    | =@pytest.fixture=                  |
| =beforeEach()=        | =@pytest.fixture(autouse=True)=    |

#+begin_src typescript
// Mocha/Chai
describe("CompletionProvider", () => {
  it("should complete module names", async () => {
    const result = await getCompletion(doc, position);
    expect(result.items).to.have.length.greaterThan(0);
  });
});
#+end_src

#+begin_src python
# pytest equivalent
import pytest
from ansible_ls.providers.completion import get_completion

class TestCompletionProvider:
    async def test_complete_module_names(self, doc, position):
        result = await get_completion(doc, position)
        assert len(result.items) > 0
#+end_src

**** Porting Tiers

***** Tier 1: Foundation (Port First)
- =interfaces/module.ts= - Data structures
- =interfaces/extensionSettings.ts= - Settings
- =utils/ansible.ts= - Keyword definitions (722 lines of dicts)
- =test/helper.ts= - Test infrastructure

***** DONE Tier 2: Core Utilities
CLOSED: [2026-01-28 Tue]
:PROPERTIES:
:ID:       187f1b86-7e65-420d-ae76-a42dfeed799b
:END:
- Note taken on [2026-01-28 Wed 17:54] \\
  Would [[https://tree-sitter.github.io/tree-sitter/][tree-sitter]] be a good fit for YAML AST navigation? Maybe with the [[https://github.com/ikatyang/tree-sitter-yaml][tree-sitter-yaml]] grammar and [[https://github.com/neomutt/lsp-tree-sitter][lsp-tree-sitter]]?

- =utils/yaml.ts= - YAML AST navigation (critical, complex)
- =utils/docsParser.ts= - Documentation parsing
- =utils/commandRunner.ts= - External command execution

****** Tree-sitter Research Findings
:PROPERTIES:
:CREATED:  [2026-01-28 Tue]
:END:

*Recommendation: Use tree-sitter-yaml with py-tree-sitter for YAML AST navigation.*

******* Installation
#+begin_src bash
pip install tree-sitter tree-sitter-languages
#+end_src

******* tree-sitter vs ruamel.yaml Comparison

| Feature              | tree-sitter-yaml             | ruamel.yaml                  |
|----------------------+------------------------------+------------------------------|
| Position Tracking    | Native (start/end points)    | Native (lc attrs)            |
| Incremental Parsing  | Yes (efficient updates)      | No                           |
| Parent Traversal     | Cursor-based                 | Direct parent refs           |
| Performance          | Optimized (C implementation) | Pure Python (slower)         |
| Language Server Fit  | Excellent                    | Good for read-only           |

******* Core Pattern: Get Node at Position
#+begin_src python
from tree_sitter_languages import get_language, get_parser

language = get_language('yaml')
parser = get_parser('yaml')

def get_node_at_position(tree, offset: int):
    """Get deepest node containing the byte offset."""
    def walk(node):
        if node.start_byte <= offset < node.end_byte:
            for child in node.children:
                result = walk(child)
                if result:
                    return result
            return node
        return None
    return walk(tree.root_node)

# Parse and query
tree = parser.parse(yaml_content.encode('utf-8'))
node = get_node_at_position(tree, cursor_offset)
print(f"Type: {node.type}, Text: {node.text.decode()}")
print(f"Position: line {node.start_point[0]}, col {node.start_point[1]}")
#+end_src

******* AncestryBuilder Port Pattern
#+begin_src python
class AncestryBuilder:
    """Port of TypeScript AncestryBuilder for tree-sitter YAML."""

    def __init__(self, path: list = None, index: int = None):
        self._path = path or []
        self._index = index if index is not None else len(self._path) - 1

    def parent(self, expected_type=None):
        """Move up path, optionally asserting type."""
        self._index -= 1
        current = self.get()
        # Skip pair nodes unless explicitly expected
        if current and current.type == 'block_mapping_pair':
            if expected_type != 'pair':
                self._index -= 1
        # Type assertion
        if expected_type:
            current = self.get()
            if not current or current.type != expected_type:
                self._index = float('-inf')
        return self

    def get(self):
        """Get current node."""
        if 0 <= self._index < len(self._path):
            return self._path[int(self._index)]
        return None
#+end_src

******* pygls Integration Example
#+begin_src python
from pygls.server import LanguageServer
from tree_sitter_languages import get_parser

class AnsibleLanguageServer:
    def __init__(self):
        self.server = LanguageServer('ansible-language-server', 'v1.0')
        self.parser = get_parser('yaml')
        self.trees = {}  # uri -> tree cache

        @self.server.feature('textDocument/didOpen')
        def did_open(params):
            uri = params.textDocument.uri
            text = params.textDocument.text
            self.trees[uri] = self.parser.parse(text.encode('utf-8'))

        @self.server.feature('textDocument/didChange')
        def did_change(params):
            uri = params.textDocument.uri
            text = params.contentChanges[0]['text']
            # Incremental parsing possible but full reparse is simple
            self.trees[uri] = self.parser.parse(text.encode('utf-8'))
#+end_src

******* lsp-tree-sitter Assessment
[[https://github.com/neomutt/lsp-tree-sitter][lsp-tree-sitter]] provides utilities for tree-sitter-based language servers:
- Converts ASTs to JSON
- Generates JSON schemas from grammars
- ~1,400 weekly downloads (small community)
- Limited YAML-specific features

*Verdict:* Optional utility layer. Custom node navigation (like =AncestryBuilder=) will still be needed.

***** Tier 3: Services
- =services/settingsManager.ts=
- =services/workspaceManager.ts=
- =services/docsLibrary.ts=
- =services/ansibleLint.ts=
- =services/validationManager.ts=

***** DONE Tier 4: Complex Services
CLOSED: [2026-01-28 Tue]
:PROPERTIES:
:ID:       b3c8a3b7-2dff-4ebe-abcb-038942d7b008
:END:
- Note taken on [2026-01-28 Wed 17:53] \\
  Is there a Python library that talks to Podman for this?
- =services/executionEnvironment.ts= - Container management (536 lines)

****** Podman Python Library Research
:PROPERTIES:
:CREATED:  [2026-01-28 Tue]
:END:

*Recommendation: Use [[https://github.com/containers/podman-py][podman-py]] (official Podman Python SDK).*

******* Installation
#+begin_src bash
pip install podman  # podman-py v5.7.0 (Jan 2026)
#+end_src

******* Library vs Subprocess Comparison

| Aspect           | podman-py (Library)          | subprocess                   |
|------------------+------------------------------+------------------------------|
| Error handling   | Structured exceptions        | Parse exit codes/stderr      |
| Type safety      | Good (typed API)             | Poor                         |
| Async support    | Yes (AsyncPodmanClient)      | Limited                      |
| Streaming        | Native                       | Manual                       |
| Performance      | ~10-20ms per op              | ~50-100ms (process spawn)    |
| Dependencies     | 1 package                    | None                         |

******* Basic Container Operations
#+begin_src python
from podman import PodmanClient

class ExecutionEnvironment:
    def __init__(self, image: str):
        self.image = image
        self.client = PodmanClient(
            base_url="unix:///run/user/1000/podman/podman.sock"
        )

    def pull_image(self) -> bool:
        """Pull container image."""
        try:
            self.client.images.pull(self.image)
            return True
        except Exception as e:
            print(f"Pull failed: {e}")
            return False

    def run_container(self, name: str) -> bool:
        """Run detached container with bash shell."""
        try:
            self.client.containers.run(
                image=self.image,
                name=name,
                command=["bash"],
                detach=True,
                stdin=True,
                environment=self._get_ansible_env(),
                volumes=self._get_volume_mounts(),
            )
            return True
        except Exception as e:
            print(f"Run failed: {e}")
            return False

    def execute_command(self, container_name: str, cmd: list) -> tuple[int, str]:
        """Execute command in running container."""
        container = self.client.containers.get(container_name)
        exit_code, output = container.exec_run(cmd=cmd, stdout=True, stderr=True)
        return exit_code, output.decode() if output else ""

    def cleanup(self, container_name: str) -> None:
        """Stop and remove container."""
        try:
            container = self.client.containers.get(container_name)
            container.stop(timeout=5)
            container.remove()
        except Exception:
            pass  # Container may not exist
#+end_src

******* docker-py Compatibility
=docker-py= can connect to Podman's Docker-compatible socket:
#+begin_src python
import docker
client = docker.DockerClient(
    base_url='unix:///run/user/1000/podman/podman.sock'
)
#+end_src
*Caveats:* =docker.from_env()= unreliable with Podman; explicit =base_url= required.

******* Hybrid Approach (Recommended)
#+begin_src python
import shutil
from podman import PodmanClient

class ExecutionEnvironment:
    def __init__(self, settings: dict):
        self.settings = settings
        self.container_engine = self._detect_engine()
        self.client = PodmanClient(...) if self.container_engine == "podman" else None

    def _detect_engine(self) -> str:
        """Detect podman or docker via shutil.which (no subprocess)."""
        for engine in ["podman", "docker"]:
            if shutil.which(engine):
                return engine
        raise RuntimeError("Neither podman nor docker found")
#+end_src

Use =podman-py= for container lifecycle, keep =subprocess= only for platform detection.

***** Tier 5: Providers
- =providers/validationProvider.ts=
- =providers/completionProvider.ts= (most complex at 666 lines)
- =providers/hoverProvider.ts=
- =providers/definitionProvider.ts=
- =providers/semanticTokenProvider.ts=

***** Tier 6: LSP Core
- =ansibleLanguageService.ts=
- =server.ts=

**** YAML AST Navigation

The =yaml.ts= module is critical. It navigates the YAML AST to determine cursor context (play vs task vs block level).

#+begin_src typescript
// TypeScript uses yaml npm package
import { parseAllDocuments, YAMLMap, Pair } from "yaml";

export function getPathAt(doc: Document, offset: number): Node[] {
  // Navigate to node at cursor position
  // Return path from root to target
}
#+end_src

#+begin_src python
# Python: ruamel.yaml preserves positions
from ruamel.yaml import YAML
from ruamel.yaml.comments import CommentedMap

def get_path_at(doc: CommentedMap, offset: int) -> list:
    """Navigate YAML AST to cursor position."""
    # ruamel.yaml nodes have lc (line/column) attributes
    # Use lc.line, lc.col for position tracking
    pass
#+end_src

**** DONE External Command Integration
CLOSED: [2026-01-28 Tue]
:PROPERTIES:
:ID:       c44785a8-6794-445f-80f4-ebc6e617eb5c
:END:

- Note taken on [2026-01-28 Wed 18:01] \\
  Do all of these have API or python library options? It would be nice to trim the process management and hit libraris.
The server spawns external processes:

| Command                          | Purpose                 | Python API?              |
|----------------------------------+-------------------------+--------------------------|
| =ansible --version=                | Detect installation     | *Yes* - importlib.metadata |
| =ansible-lint=                     | Playbook linting        | *No* - subprocess only     |
| =ansible-playbook --syntax-check=  | Fallback validation     | *Limited* - unstable API   |
| =podman/docker run=                | Containerized execution | *Yes* - podman-py          |

***** API Availability Research
:PROPERTIES:
:CREATED:  [2026-01-28 Tue]
:END:

****** ansible --version → importlib.metadata (Recommended)
#+begin_src python
import importlib.metadata
import sys

def get_ansible_metadata() -> dict:
    """Get Ansible version info without subprocess."""
    metadata = {}

    try:
        metadata['ansible_version'] = importlib.metadata.version('ansible')
    except importlib.metadata.PackageNotFoundError:
        metadata['ansible_version'] = None

    try:
        metadata['ansible_core_version'] = importlib.metadata.version('ansible-core')
    except importlib.metadata.PackageNotFoundError:
        metadata['ansible_core_version'] = None

    metadata['python_version'] = f"{sys.version_info.major}.{sys.version_info.minor}"

    try:
        import ansible
        import os
        metadata['ansible_location'] = os.path.dirname(ansible.__file__)
    except ImportError:
        metadata['ansible_location'] = None

    return metadata
#+end_src

****** ansible-lint → Subprocess Only
*No public Python API.* The project explicitly states it's not a library.
- CLI with JSON output (=-f codeclimate=) is the stable interface
- Keep subprocess approach; parse JSON output
#+begin_src python
import subprocess
import json

def run_ansible_lint(playbook_path: str, config_path: str = None) -> list:
    """Run ansible-lint and parse JSON output."""
    cmd = ["ansible-lint", "-f", "codeclimate", "--offline", "--nocolor"]
    if config_path:
        cmd.extend(["-c", config_path])
    cmd.append(playbook_path)

    result = subprocess.run(cmd, capture_output=True, text=True)
    if result.stdout:
        return json.loads(result.stdout)
    return []
#+end_src

****** ansible-playbook --syntax-check → Subprocess Recommended
Ansible's Python API exists but is *officially unsupported for external use*:
#+begin_quote
"This API is intended for internal Ansible use. Ansible may make changes to this API at any time that could break backward compatibility."
— [[https://docs.ansible.com/projects/ansible-core/2.17/dev_guide/developing_api.html][Ansible Core Python API Docs]]
#+end_quote

*Thread safety warning:* "Because Ansible relies on forking processes, this API is not thread safe."

Keep subprocess for syntax-check — more stable for a multi-threaded language server.

****** ansible-doc → ansible-runner Library
=ansible-runner= provides a stable Python interface for documentation:
#+begin_src python
import ansible_runner
import json

def get_module_docs(module_name: str) -> dict:
    """Get module documentation via ansible-runner."""
    output, errors = ansible_runner.get_plugin_docs(
        plugin_names=[module_name],
        plugin_type='module',
        response_format='json'
    )
    return json.loads(output) if output else {}

def list_modules() -> list:
    """List available modules."""
    output, errors = ansible_runner.get_plugin_list(
        list_files=False,
        response_format='json'
    )
    return json.loads(output) if output else []
#+end_src

****** Summary: What to Use
| Command              | Approach                | Rationale                        |
|----------------------+-------------------------+----------------------------------|
| Version detection    | =importlib.metadata=      | No subprocess, fast, reliable    |
| ansible-lint         | subprocess + JSON parse | Only stable interface            |
| Syntax check         | subprocess              | Thread-safe, stable              |
| Module docs          | =ansible-runner=          | Stable Python API                |
| Container management | =podman-py=               | Type-safe, async support         |

**** Python Project Structure

#+begin_src
ansible-language-server-py/
├── pyproject.toml
├── src/
│   └── ansible_ls/
│       ├── __init__.py
│       ├── server.py              # Entry point
│       ├── language_service.py    # LSP orchestrator
│       ├── models/
│       │   ├── __init__.py
│       │   ├── module.py          # ModuleMetadata, Option
│       │   └── settings.py        # ExtensionSettings
│       ├── providers/
│       │   ├── __init__.py
│       │   ├── completion.py
│       │   ├── hover.py
│       │   ├── definition.py
│       │   ├── validation.py
│       │   └── semantic_tokens.py
│       ├── services/
│       │   ├── __init__.py
│       │   ├── workspace.py
│       │   ├── docs_library.py
│       │   ├── execution_env.py
│       │   ├── settings.py
│       │   └── validation.py
│       └── utils/
│           ├── __init__.py
│           ├── yaml_utils.py
│           ├── ansible_keywords.py
│           ├── docs_parser.py
│           └── command_runner.py
└── tests/
    ├── conftest.py
    ├── fixtures/
    ├── providers/
    └── utils/
#+end_src

**** DONE pyproject.toml Skeleton
CLOSED: [2026-01-28 Tue]
:PROPERTIES:
:ID:       65ba55a4-8fa8-4639-8e6a-6dbe276f8cf3
:END:

- Note taken on [2026-01-28 Wed 18:08] \\
  Address this warning
  #+begin_src shell :results output :wrap example
  uv sync

  #+end_src

  #+RESULTS:
  #+begin_example
  warning: Skipping installation of entry points (`project.scripts`) because this project is not packaged; to install entry points, set `tool.uv.package = true` or define a `build-system`
Using CPython 3.14.2
Creating virtual environment at: .venv
Resolved 22 packages in 8.12s
Prepared 4 packages in 1.17s
Installed 6 packages in 6ms
 + attrs==25.4.0
 + cattrs==25.3.0
 + lsprotocol==2025.0.0
 + pygls==2.0.1
 + ruamel-yaml==0.19.1
 + typing-extensions==4.15.0
  #+end_example

***** Fix: Add build-system for Entry Points
The warning occurs because =uv= only packages projects with a =[build-system]= defined.
Adding Hatchling (PyPA-recommended, uv's default) enables entry point installation.

#+begin_src toml :tangle pyproject.toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ansible-language-server"
version = "0.1.0"
description = "Language Server Protocol implementation for Ansible"
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}
dependencies = [
    "pygls>=1.3.0",
    "lsprotocol>=2023.0.0",
    "ruamel.yaml>=0.18.0",
    "attrs>=23.0.0",
    "tree-sitter>=0.23.0",
    "tree-sitter-languages>=1.10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
]
container = [
    "podman>=5.0.0",
]
ansible = [
    "ansible-runner>=2.4.0",
]

[project.scripts]
ansible-language-server = "ansible_ls.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ansible_ls"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
#+end_src

***** Installation with Entry Points
After adding =[build-system]=:
#+begin_src bash
uv sync                    # Install dependencies
uv pip install -e .        # Install project with entry points
ansible-language-server --version  # Verify CLI works
#+end_src

***** Build System Options
| Build System | Best For                          | uv Integration |
|--------------+-----------------------------------+----------------|
| Hatchling    | General Python projects (default) | Excellent      |
| Setuptools   | Complex packages with C ext       | Good           |
| uv_build     | Pure Python, minimal config       | Best           |

Hatchling recommended for language servers — production-ready, minimal config.

**** Risk Assessment (Updated)

| Area                    | Risk   | Mitigation                                        |
|-------------------------+--------+---------------------------------------------------|
| YAML position tracking  | Medium | tree-sitter-yaml with native position API         |
| Container management    | Low    | podman-py typed API with structured errors        |
| Completion context      | Medium | Port tests first, validate behavior               |
| Async patterns          | Low    | pygls handles async well, use pytest-asyncio      |
| Module documentation    | Low    | ansible-runner provides stable Python API         |
| External commands       | Low    | importlib.metadata for version; subprocess stable |

**** TODO Next Steps
:PROPERTIES:
:ID:       042d8753-3757-48a6-b5a0-251196171795
:END:
Research phase complete. Ready to proceed with implementation.

1. [X] Initialize Python project with =uv init=
2. [X] Research tree-sitter for YAML AST (see Tier 2 findings)
3. [X] Research podman-py for container management (see Tier 4 findings)
4. [X] Research Ansible Python APIs (see External Command Integration)
5. [X] Fix pyproject.toml build-system for entry points
6. [ ] Set up pytest infrastructure with fixtures from =test/helper.ts=
7. [ ] Port =utils/ansible.ts= keyword definitions (low risk, high value)
8. [ ] Port =interfaces/module.ts= to dataclasses
9. [ ] Port =utils/yaml.ts= using tree-sitter-yaml with comprehensive tests
10. [ ] Incrementally port services and providers

***** Revised Dependency List
Based on research, the Python port should use:
#+begin_src
pygls>=1.3.0              # LSP framework
lsprotocol>=2023.0.0      # LSP types
tree-sitter>=0.23.0       # Parser framework
tree-sitter-languages>=1.10.0  # Pre-built grammars (incl. YAML)
ruamel.yaml>=0.18.0       # YAML round-tripping (if needed)
attrs>=23.0.0             # Data classes alternative
podman>=5.0.0             # Container management [optional]
ansible-runner>=2.4.0     # Module docs API [optional]
#+end_src

